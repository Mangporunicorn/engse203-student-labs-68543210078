# AI Usage Log — สอบกลางภาค

ชื่อ-รหัส: ปริยากร ธารพรศรี 68543210078-0

บันทึกทุกครั้งที่ใช้ AI ระหว่างสอบ

| เวลา | งาน (B1/B2/B3/B4) | ถาม AI ว่าอะไร | ใช้คำตอบส่วนไหน | แก้เอง/ตรวจสอบอย่างไร |
|---|---|---|---|---|
| 11:05 | B1 | Console ขึ้นข้อความเตือนเรื่อง key ต้องแก้ยังไง | เอาวิธีเพิ่ม key ให้แต่ละรายการที่แสดงหลายอัน | เข้าไปแก้ไฟล์ RequestList.jsx เพิ่ม `key={request.id}` แล้วลองรีเฟรชหน้า ดูว่าข้อความเตือนหายไหม |
| 11:12 | B1 | ทำไมเลขรอดำเนินการถึงผิด | ใช้คำแนะนำว่าตรง pending ไปนับ completed ผิด | แก้จาก `completed` เป็น `pending` แล้วดูหน้าเว็บว่าตัวเลขเป็น 5 / 3 / 1 / 1 หรือไม่ |
| 11:18 | B1 | กดตัวกรองรอดำเนินการแล้วรายการผิด ต้องแก้ตรงไหน | ใช้วิธีเปลี่ยนการเช็กจากไม่เท่ากันเป็นเท่ากัน | แก้ใน DashboardPage.jsx แล้วลองกดรอดำเนินการ ดูว่าเหลือ 3 รายการไหม |
| 11:25 | B1 | เปลี่ยน URL แล้วข้อมูลไม่เปลี่ยน ต้องแก้ยังไง | ใช้วิธีเพิ่ม `requestId` เข้าไปในส่วนที่ใช้ตรวจการเปลี่ยนแปลง | แก้ RequestDetailPage.jsx แล้วลองเปลี่ยนจาก REQ-001 เป็น REQ-002 โดยไม่กดรีเฟรช |
| 11:35 | B1 | กดลบแล้วรายการยังอยู่ ต้องแก้ตรงไหน | ใช้คำแนะนำให้เอาข้อมูลใหม่หลังลบมาใส่ในตัวแปรที่ใช้แสดงหน้าเว็บ | แก้เป็น `setRequests(nextRequests)` แล้วลองลบการ์ดและดูว่าหายทันทีไหม |
| 11:45 | B1 | กด Reset แล้วหน้าพัง ต้องแก้ยังไง | ใช้วิธีรอให้ `resetRequests()` ทำงานเสร็จก่อน แล้วค่อยเอาข้อมูลมาแสดง | แก้โดยใช้ `await` แล้วลองกด Reset ดูว่าข้อมูลกลับมา 5 รายการและหน้าไม่พัง |
| 12:48 | B2 | ช่องค้นหาต้องเพิ่มตรงไหนและใช้ useState ยังไง | วิธีสร้างตัวแปรไว้เก็บคำที่พิมพ์ และทำช่อง input ให้รับข้อความ | ฉันเพิ่ม searchText กับ setSearchText แล้วลองพิมพ์ "ห้อง" ในช่อง พบว่าพิมพ์ได้จริง |
| 12:55 | B2 | ทำให้ช่องค้นหาค้นจากชื่อผู้แจ้งหรือรายละเอียดได้ยังไง | วิธีใช้ `filter()` และ `includes()` เพื่อค้นหาข้อความ | ฉันแก้ DashboardPage.jsx แล้วลองพิมพ์ "ห้อง" ได้ REQ-002 กับ REQ-003 ตรงตามโจทย์ |